<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'ScrapGenius' %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link href="/css/style.css" rel="stylesheet">
</head>
<body style="background-color: #fff;">
  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #fff;">
    <div class="container">
      <a class="navbar-brand" href="/" style="color: #000;">ScrapGenius</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/materials" style="color: #000;">Materials</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/market" style="color: #000;">Market</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about" style="color: #000;">About</a>
          </li>
        </ul>
        <div class="d-flex">
          <% if (session.user) { %>
            <a href="/profile" class="btn btn-outline-primary me-2">Profile</a>
            <a href="/logout" class="btn btn-outline-danger">Logout</a>
          <% } else { %>
            <a href="/login" class="btn btn-custom-login">Login</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>